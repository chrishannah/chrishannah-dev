---
layout: base.njk
---

<article>
    <h2 class="post-title">{{ title }}</h2>
    {% if date %}
      <time datetime="{{ date }}">{{ date | date("MMMM D, YYYY") }}</time>
    {% endif %}
    {{ content | safe }}
</article>
